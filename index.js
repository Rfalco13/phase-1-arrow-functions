const divide = function (numerator, denominator) {
    return numerator / denominator;
  };
  
const square = (num) => {
    return num * num;
}
  
const add = (num1, num2) => {
    return num1 + num2;
};
